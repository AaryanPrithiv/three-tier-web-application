# Dev overlay (kind/minikube): faster iteration, fewer replicas.
frontend:
  replicaCount: 1
  pdb:
    enabled: false
backend:
  replicaCount: 1
  pdb:
    enabled: false
ingress:
  enabled: true
  className: nginx
